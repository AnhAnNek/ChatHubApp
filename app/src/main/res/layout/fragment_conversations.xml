<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.example.chat.conversations.ConversationViewModel" />

        <variable
            name="conversationsAdapter"
            type="com.example.chat.conversations.adapter.ConversationsAdapter" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/color_background_primary">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_friends"
                setupOverScroll="@{true}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adapter="@{conversationsAdapter}"
                android:padding="@dimen/_5sdp"
                android:visibility="@{viewModel.isLoading ? View.GONE : View.VISIBLE}"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

            <ProgressBar
                android:id="@+id/pb_load"
                android:layout_width="@dimen/_25sdp"
                android:layout_height="@dimen/_25sdp"
                android:layout_gravity="center"
                android:visibility="@{viewModel.isLoading ? View.VISIBLE : View.GONE}" />

            <TextView
                android:id="@+id/txv_message_error"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginStart="@dimen/_20sdp"
                android:layout_marginEnd="@dimen/_20sdp"
                android:gravity="center"
                android:text="You have no conversation"
                android:visibility="@{viewModel.isShowMessageError ? View.VISIBLE : View.GONE}" />

        </FrameLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_add_chat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_margin="@dimen/_10sdp"
            android:tint="@color/color_edit_text_border"
            android:backgroundTint="@color/color_primary_link_blue"
            android:onClickListener="@{() -> viewModel.navigateToFriends()}"/>

    </RelativeLayout>

</layout>